// Project type definition
/*
type Project = {
  id: string,
  name: string,
  notes: string,
  taskIds: string[],
  createdAt: number
}

// Updated Task type to include projects
type Task = {
  id: string,
  name: string,
  column: string,
  isComplete: boolean,
  previousColumn: string,
  notes: string,
  subTasks: SubTask[],
  projectIds: string[]  // New field
}
*/ 